import styles from './HomePage.module.scss';

const HomePage = () => {

  return (
    <section className={styles.home_wrapper}>
      <h1 className={styles.home_title}>Home Page</h1>
    </section>
  );
};

export default HomePage;
// import { PayloadAction } from "@reduxjs/toolkit/dist/createAction";
// import { EventModel } from "../domain/events";

export const reducers = {
  // createEvent(state: EventModel, action: PayloadAction<EventModel>) {
 
  // },
};

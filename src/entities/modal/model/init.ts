import {  ModalInitState } from "./types";

const initialState: ModalInitState = {
  eventCurrentMenegment: 0,
  isEventModalActive: false,
  eventCurrentId: null,
};

export { initialState };

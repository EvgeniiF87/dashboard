import { TagInitState } from "./types";

const initialState: TagInitState = {
  currentEventPlaceTag: null,
  currentTag: null,
  loading: false,
  error: null,
};

export { initialState };
